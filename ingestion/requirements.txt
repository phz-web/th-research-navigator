# Top-level pinned dependencies for thrn-ingest
# Install with: pip install -r requirements.txt
#
# Python 3.11+ required.

psycopg[binary,pool]>=3.2
requests>=2.32
tenacity>=9.0
typer>=0.12
python-dotenv>=1.0
typesense>=0.21
orjson>=3.10

# Dev / test
pytest>=8.0
